# Holdout fixtures — actix/actix-web. NEVER tune the ranker against these tasks.
# Clone: git clone https://github.com/actix/actix-web.git
# SHAs are real commits from upstream history.
tasks:
  - id: actix-t1
    sha: f31f9bc92c2518e40fecd7eaa711b0bdf01ec0b9
    description: "fix(files): do not panic on pre-EPOCH files (#3922)"
    ground_truth_files:
      - actix-files/src/named.rs
    cr_query: "actix-files panics when serving a file with mtime before the unix epoch"
    cr_mode: debug

  - id: actix-t2
    sha: d3bf929040129241d9141dfce9826c40acec549f
    description: "fix: panic when .to()/.service() called after .wrap() on Route (#3944)"
    ground_truth_files:
      - actix-web/src/route.rs
    cr_query: "Route panics when .to() or .service() is called after .wrap() middleware"
    cr_mode: debug

  - id: actix-t3
    sha: 41e4863748ac0c8e446d96528fa1c31b2b20aae1
    description: "fix(awc): do not request as chunked if body is empty (#3910)"
    ground_truth_files:
      - awc/src/client/h1proto.rs
    cr_query: "awc h1 client sends Transfer-Encoding chunked even when request body is empty"
    cr_mode: debug
