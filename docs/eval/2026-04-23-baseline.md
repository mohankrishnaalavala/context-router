# Synthetic Recall Baseline — 2026-04-23

## Setup

- Fixture: `tests/fixtures/workspaces/synthetic/` (10 queries, 2-repo checkout workspace)
- Command: `context-router eval --queries .../queries.jsonl --project-root .../synthetic --k 20`
- Index: workspace root indexed with `context-router init && context-router index`

## Results

| Metric | Value |
|---|---|
| Recall@20 | **1.000** |
| n_queries | 10 |
| mean_pack_tokens | 952 |
| token_efficiency | 0.000* |

*token_efficiency = recall×1000/tokens = 1.0×1000/952 = 1.05 (report bug: formula needs update)

## Diagnosis

All 10 queries hit. The synthetic fixture is small (11 files, 19 symbols) so every gold
file appears naturally in the top-20 pack without any cross-repo boost.

**Task 14 (recall fixes): SKIPPED** — baseline Recall@20 = 1.0 ≥ 0.65 threshold.

## Path normalization fix applied

The runner now resolves `fixture_root` to an absolute path before stripping prefixes
from the Orchestrator's absolute-path returns. Without this fix recall was 0.0 even
when the correct files were returned.

## Notes

- Cross-repo edges from `workspace sync`: 0 (synthetic backend has no HTTP framework
  decorators so `api_endpoints` table is empty; reconcile produces no edges — expected
  for a minimal stub fixture)
- The `use_workspace` MCP flag and WorkspaceOrchestrator infrastructure are in place
  (Tasks 11-12) but not yet wired into the eval CLI path; multi-repo boost will be
  measured in a follow-up eval once real repos are populated
