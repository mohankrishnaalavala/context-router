[project]
name = "context-router-adapters-claude"
version = "4.3.0"
requires-python = ">=3.12"
description = "Claude Code agent adapter for context-router (Phase 6)"
license = {text = "Apache-2.0"}
dependencies = ["context-router-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adapters_claude"]
