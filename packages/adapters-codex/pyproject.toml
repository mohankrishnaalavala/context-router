[project]
name = "context-router-adapters-codex"
version = "0.4.0"
requires-python = ">=3.12"
description = "Codex agent adapter for context-router (Phase 6)"
dependencies = ["context-router-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adapters_codex"]
