[project]
name = "context-router-adapters-copilot"
version = "0.3.0"
requires-python = ">=3.12"
description = "GitHub Copilot agent adapter for context-router (Phase 6)"
dependencies = ["context-router-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/adapters_copilot"]
